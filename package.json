{
  "name": "aenternis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Aenternis - 3D toroidální simulace s programovatelnými entitami jako 8bit mikropočítači",
  "scripts": {
    "dev": "vite",
    "dev:p8": "vite --open /prototypes/08-viewer-3d/",
    "dev:wasm": "vite --open /web/",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:mutation": "stryker run",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "check": "npm run typecheck && npm run test:cov && npm run test:mutation"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/three": "^0.170.0",
    "@vitest/coverage-v8": "^4.1.5",
    "happy-dom": "^20.9.0",
    "typescript": "^5.7.2",
    "vite": "^7.0.0",
    "vitest": "^4.1.5"
  }
}
